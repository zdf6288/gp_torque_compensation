controller_manager:
  ros__parameters:
    update_rate: 50  # Hz

    cpp_relayer:
      type: cpp_relayer/CPPRelayer

    update_rate_diagnostic:
      type: cpp_relayer/UpdateRateDiagnosticController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster
    
    franka_robot_state_broadcaster:
      type: franka_robot_state_broadcaster/FrankaRobotStateBroadcaster

franka_robot_state_broadcaster:
  ros__parameters:
    arm_id: panda

cpp_relayer:
  ros__parameters:
    arm_id: panda
    command_timeout_sec: 0.2
    # Decouple the 1 kHz ros2_control loop from Python controller callbacks.
    # cpp_relayer.update() still writes effort each update; only /state_parameter is throttled.
    state_parameter_publish_rate: 50.0
    # 真机启动时 Python controller 可能尚未发布第一条 /effort_command。
    # false 允许 cpp_relayer 激活后使用 zero fallback 等待 fresh command，避免 activation deadlock。
    # command_timeout_sec=0.2 仍保留 stale command zeroing；这里不启用 GP/historical active compensation。
    require_fresh_command_on_activate: false
    # Diagnostics only: confirm whether Python /effort_command publish jitter triggers stale fallback.
    diagnostics_enabled: true
    diagnostics_log_period_sec: 5.0
    log_first_n_stale_events: 5

update_rate_diagnostic:
  ros__parameters:
    diagnostics_enabled: true
    diagnostics_log_period_sec: 5.0
    expected_update_rate_hz: 1000.0
    expected_publish_rate_hz: 50.0
    warn_ratio_low: 0.8
    warn_ratio_high: 1.2
